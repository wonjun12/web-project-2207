[인터페이스(interface)]
    - 어떤 객체가 있고 그 객체가 특정한 인터페이스를 사용한다면 그 객체는 반드시 인터페이스의 메소드들을 구현해야 한다.
        만약, 강제하고 잇는 메소드를 구현하지 않으면 컴파일 조차 안된다.
    - 서로 프로젝트를 진행할때 실수를 줄이기 위해 사용되며 보다 호율적인 커뮤니티를 위해 사용해야한다.
    - 혼자 사용할때는 많이 사용 안할수 있지만 다르게 생각하면 초안을 만들어 놓고 다른 클래스에서 작업한다고 해도 좋을수 있다.
    - 인터페이스(interface) 내부에서 기본적은 변수 선언은 안되자만 상수용으로는 선언이 가능하다.
        (해당 변수는 static final 자동 선언된다.)

    ex) 
        //인터페이스를 사용할때는 기초공사를 위해 초안을 말한다.
        interface I {
            public void z(); //메소드 값이 없어야한다.
        }       

        //초안을 보고 구현하기 위해 공사를 시작한다는 의미다.
        class A implements I {
            public void z() {} //해당 메소드가 없다면 컴파일 오류가 뜬다.
                // 구현 해야한다.
        }